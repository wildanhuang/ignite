<script type="javascript">
  $(function () {
    $('[data-toggle="tooltip"]').tooltip()
  })
</script>

<% if current_user %>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <% link_to '/' do %><%= image_tag 'logo.png', width: '100', height: '100' %><% end %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <%= link_to 'Spirit Charge', spirit_charges_path, class: 'btn btn-outline-secondary' %>
        </li>
        <!-- <li class="nav-item active">
          <a class="nav-link" href="#">Link</a>
        </li> -->
        &nbsp;
        <li class="nav-item dropdown active">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Menu
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%= link_to 'My Readings', readings_path, class: 'dropdown-item' %>
            <div class="dropdown-divider"></div>
            <%= link_to 'My Profile', user_path(current_user), class: 'dropdown-item' %>
            <% if can? :manage, User  %>
              <%= link_to 'Admin - User List', users_path, class: 'dropdown-item' %>
            <% end %>
          </div>
        </li>
        <!-- <li class="nav-item">
          <a class="nav-link disabled" href="#">Disabled</a>
        </li> -->
      </ul>

      <form class="form-inline my-2 my-lg-0">
        Welcome, <%= "#{current_user.firstname} #{current_user.lastname}" %>
        &nbsp;

        <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: "btn btn-outline-danger my-2 my-sm-0" %>
      </form>
    </div>
  </nav>
<% end %>
